# This file allows you to configure how the build.sh script operates. It will be sourced immediately before running a
# build command. Functions and variables in this file will override those set by build.sh.
