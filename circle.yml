python:
        version:
                - 2.7
                - 3.4
                - pypy-2.2.1
dependencies:
        post:
                - pip install -r requirements -r requiements-dev.txt
                - python setup.py develop
test:
        override:
                - mamba --enable-coverage
        post:
                - 'coverage report --include="mamba/*","spec/*"'

